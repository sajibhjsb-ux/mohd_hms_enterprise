# Task ID: 33-frontend — Email Automation System UI (Settings → Email)

Agent: Z.ai Code (email-admin-frontend)
Date: 2026 (session)

## Task
Build the "Email" administration tab inside the EXISTING Settings module (no new top-level module): Overview (health, SMTP status, test tools, SMTP configuration), Templates, Automations, Logs — permission-gated via `PERMISSIONS.email_view / email_config / email_templates / email_automations / email_actions` from `@/lib/hms/constants`.

## Work Log (concrete steps)
1. RECON: read `worklog.md` tail (task 30/31/32 conventions), `settings/index.tsx` + `automation-tab.tsx` (module/tab patterns, toasts, Switch/Table usage), `session.tsx` (`hasPerm`/`useSession`), `lib/hms/api-client.ts` (`api.get/post/patch`, `qs`, `ClientApiError.message`), `shared/ui-bits.tsx` (`PageHeader/LoadingState/ErrorState/EmptyState`), shadcn component inventory. Confirmed all five `email_*` permissions already exist in `constants.ts` (lines 109–113) — no constants change needed.
2. CREATED `src/components/hms/modules/settings/email-tab.tsx` — `EmailTab` container (Tabs: Overview | Templates | Automations | Logs; sub-tab triggers per perm) + `OverviewPanel`: health stat tiles + last success/failure from `GET /api/v1/email/health` ("—" when null, Refresh), SMTP status card + "Test connection" (`POST /api/v1/email/config/test-connection`, inline green/red + toast, `email-test-connection`), send-test-email card (`POST /api/v1/email/config/test-send`, recipient defaults to `config.testRecipient`, optional template Select, `email-test-send`), config form (`email_config` only; write-only `smtpPassword` with "Stored — leave blank to keep" placeholder, only PATCHed when typed; "Clear stored" PATCHes `smtpPassword:null`; `email-config-save`), read-only summary without the perm, module mailboxes read-only with Company-tab hint.
3. CREATED `email-templates.tsx` — `EmailTemplates`: debounced search/category list, version + category + critical/system badges, active Switch (critical → disabled + "Security-critical" tooltip), large editor Dialog (create + edit + view-only modes; variable tag input pre-seeded with existing + 6 globals; caret-accurate `{{VAR}}` chip insertion; `<p>/<strong>/<ul>/<a>/<hr>` selection-wrapping helpers; sandboxed preview iframe with "PREVIEW — sample data" + warnings; save = POST/PATCH with verbatim 400 toasts), test-send dialog, duplicate, version-history dialog with per-version body iframe.
4. CREATED `email-automations.tsx` — `EmailAutomations`: try/catch-safe JSON-string parsers, humanized recipient summaries, recent ✓/✗/◷ badges, Paperclip attachment count, delay "+Nm", enabled Switch (critical + non-SUPER_ADMIN → disabled+tooltip; 403 message toasted verbatim; `email-automation-toggle`), full edit/create dialog (event/template Selects, recipient-rule editor with kind-dependent value control, sender identity, delay/maxAttempts/dedupeHours, condition rows with add/remove, attachmentKinds checkboxes; `email-automation-edit/-save`), duplicate (name+" (copy)"), paginated History dialog (`meta.total`).
5. CREATED `email-logs.tsx` — `EmailLogs`: status/category/recipient/search/date-from/to filters + Clear, debounced okList fetch with `meta.total`, mandated status color map + TEST badge + AlertTriangle error indicator, keyboard-accessible row click → **detail view replacing the table** (Message ID, CC/BCC, sender, reply-to, template key+version, automation, related, attempts, timestamps, red last error, mono provider response, attachmentRefs list, sandboxed rendered body or honest "pruned after the retention window" note; `email-log-detail`), Retry/Cancel actions gated by `email_actions` with the specified disabled states (`email-log-retry/-cancel`).
6. EDITED `settings/index.tsx` (only this existing file): import `EmailTab`, `canViewEmail` gate, conditional TabsTrigger/TabsContent between Automation and Legal.
7. QA/INCIDENT: `bun run lint` clean; `bunx tsc --noEmit` — zero errors in `settings/*` (fixed two: `||`/`??` parens in automations, `srcDoc` null-type in templates; remaining tsc errors are in the concurrent backend agent's `email/*` files + pre-existing irms/scripts files — out of scope). Shared Next dev server found DOWN on :3000 mid-task → restarted with `nohup bun run dev >> dev.log 2>&1` (Task-30 precedent), verified HTTP 200, no compile errors. All 16 required data-testids verified present via grep.

## Stage Summary
- Settings now has a complete, permission-gated Email admin area matching automation-tab.tsx visual language (emerald/amber/red/neutral only, no new deps, no cross-panel imports).
- Files produced: `email-tab.tsx` (564 lines), `email-templates.tsx` (745), `email-automations.tsx` (781), `email-logs.tsx` (474) + minimal edit to `settings/index.tsx`.
- Contract notes for 33-backend (IMPORTANT): config GET returns `hasPassword` never the password; `smtpPassword` PATCH accepts string|null; automations POST/PATCH payloads are STRUCTURED (`recipientRule:{kind,value?}`, `conditions:[{field,op,value}]`, `attachments:[kind]`) while GET rows serialize the same as JSON strings; `templateId` on test-send = template KEY from meta; lists use okList + `meta.total`; meta must expose events/roles/recipientKinds/conditionOps/attachmentKinds/categories/templates/moduleMailboxes; template create = `POST /api/v1/email/templates`.
- Deviations: none of substance. "New template" implemented as POST `/api/v1/email/templates` (spec listed the button without endpoint); Logs trigger gated by `email_view` (per its constants comment "access the email admin area, logs and health"); test connection/send cards gated by `email_config` (per its permission description).
