# Task 7 — Petty Cash (spec §31–§41) — Work Record

## Files owned & written
- API `src/app/api/v1/finance/petty-cash/`:
  - `funds/route.ts` — GET list (finance_read; custodian, approved in/out aggregates, pending + tx counts, search/status/pagination) / POST create (finance_manage; code `nextNumber("PCF")`, `currentBalanceCents=openingBalanceCents`, custodian must be ACTIVE staff, case-insensitive duplicate name → 409, opening ≥ 0, audit `PETTY_CASH_FUND_CREATED`)
  - `funds/[id]/route.ts` — GET detail (fund + custodian + approved totalIn/totalOut + pendingCount + recent 25 txs + last 10 reconciliations) / PATCH (finance_manage; name/custodian/notes/status; deactivate with PENDING txs → 409 "Settle pending transactions first."; duplicate-name guard; audit `PETTY_CASH_FUND_UPDATED`)
  - `transactions/route.ts` — GET (finance_read; filters fundId/type/status/month YYYY-MM via txDate gte/lt/requesterId; listQuery/pagedMeta; include fund{code,name}; txDate desc) / POST (finance_manage; direction SERVER-DERIVED: CASH_IN/TOP_UP→IN, CASH_OUT/EXPENSE/REIMBURSEMENT→OUT, ADJUSTMENT→body default OUT; EXPENSE/REIMBURSEMENT require description; requesterName resolved from requesterId or creator; status PENDING; code `nextNumber("PCT")`; fund must be ACTIVE; dedupeSubmission; audit `PETTY_CASH_TX_CREATED`). A create NEVER moves the balance.
  - `transactions/[id]/route.ts` — GET (finance_read OR creator) / PATCH (finance_manage, PENDING only; amount/category/description/txDate/reference/requesterId/type; direction re-derived; audit `PETTY_CASH_TX_UPDATED`; APPROVED/REJECTED → 422)
  - `transactions/[id]/transition/route.ts` — POST {action approve|reject} (finance_manage; dedupeSubmission keyed {id,action}). reject: note ≥5 chars → REJECTED + reviewNote, balance untouched. approve: SEPARATION OF DUTIES — creator cannot self-approve unless ADMIN/SUPER_ADMIN → 403 "You cannot approve your own petty cash transaction."; inside ONE `db.$transaction`: newBalance = current ± amountCents, OUT && newBalance<0 → `Errors.invalidTransition("Insufficient fund balance...")` BEFORE any write; fund balance updated + tx APPROVED (approvedById/At, balanceAfterCents snapshot); ledger mirror in SAME tx (ACC-CASH account increment/decrement + Transaction row `nextNumber("TRX")`, type INCOME|EXPENSE, description `Petty cash {code} — {fund.name}`, referenceType PETTY_CASH; skipped best-effort if ACC-CASH missing); second approve → 422 via status guard; audit `PETTY_CASH_TX_APPROVED {code,amountCents,direction,balanceAfterCents}`
  - `transactions/[id]/receipt/route.ts` — POST multipart (finance_manage OR creator while PENDING; ≤10MB; pdf/jpg/jpeg/png; magic-byte sniff `%PDF-`/`89504e47…`/`ffd8ff`; ext must agree; key `finance/petty-cash/{fundId}/{txId}/{uuid}.{ext}` via storage.put; replace drops superseded object; audit `PETTY_CASH_RECEIPT_ATTACHED`) / GET authorized stream (finance_read OR creator; `Content-Disposition: attachment`; absent → 404; audit `PETTY_CASH_RECEIPT_DOWNLOADED`)
  - `reconcile/route.ts` — POST (finance_manage; expected=fund.currentBalanceCents; difference=counted−expected; flagged=difference≠0; NEVER adjusts balance §39; audit `PETTY_CASH_RECONCILED`) / GET (finance_read; fundId/flagged filters, pagination)
  - `summary/route.ts` — GET (finance_read): funds(+custodian, approved in/out, pending), monthly last 6 months approved IN/OUT (JS-computed, SQLite-safe), byCategory top10 (approved OUT), byRequester top10 (EXPENSE/REIMBURSEMENT), pendingCount
  - `custodians/route.ts` — GET staff directory for pickers (FINANCE lacks users.read; returns only id/name/email/role of ACTIVE non-CUSTOMER staff)
- UI `src/components/hms/modules/finance/`: `petty-cash-tab.tsx` (exports `PettyCashTab` — NO props, self-fetching; sub-tabs Funds/Transactions/Reconciliation/Reports; pending-count badge on Transactions), `petty-cash-shared.tsx` (types, SignedAmount green/red, TxTypeBadge, ReceiptChip, FundChip), `petty-cash-transactions.tsx` (server filters fund/type/status/month; DataTable; New Transaction dialog → creates PENDING → honest toast "Transaction submitted — awaiting Finance approval." → optional receipt step; inline Approve/Reject on PENDING rows; reject requires note ≥5; receipt attach/replace via hidden file input), `petty-cash-reconciliation.tsx` (fund select → transparent math "Opening + In − Out = Expected" from API aggregates; counted input; live difference; submit → banner "Reconciled — balanced" / "Difference of BND X.XX flagged for review"; history table FLAGGED/RECONCILED badges), `petty-cash-reports.tsx` (Monthly summary, Fund balances, By category, By requester — all money() through summary API)
- QA `scripts/petty-cash-qa.ts` — 78-assertion suite against live :3000; DELETED after green run (temp QA cleanup convention)

## Fixes made during verification
1. `transactions/[id]/receipt/route.ts` POST used positional URL parsing (`pathname.split("/")[6]` = "receipt", not the id) → every receipt upload 404'd. Now uses `await ctx.params` like sibling routes.
2. QA script imported `src/lib/hms/storage` which is `import "server-only"`-guarded (unresolvable/throwing under plain bun) → replaced with an inline minio Client using the identical env contract, for cleanup only.
3. Added devDependency `server-only@0.0.1` (Next.js-standard package already imported by `src/lib/hms/storage/index.ts` + `workflows/idempotency.ts`; makes scripts able to import shared server modules — kept).
4. QA corrections: sleeps 5.3s between intentional same-action transitions (dedupeSubmission window is 5s keyed {userId,route,{id,action}}) and aggregates expect APPROVED-only flows (in=20000 not 22000 — the CASH_IN 20 guard tx is PENDING).

## Environment notes for next agents
- **Object-storage mini-service (:3090) dies between tool calls in this sandbox** (background processes are reaped when the Bash tool call ends; only the system-managed dev server persists). Receipt upload/download QA MUST start it and run tests in the SAME tool call: `cd mini-services/object-storage && setsid nohup bun index.ts &` then wait for `curl 127.0.0.1:3090`. When it is down, receipt POST honestly returns 500 "Object storage is unavailable — the receipt was not saved." and dev.log shows `storage.put.failed` ECONNREFUSED — the app degrades honestly.
- Dev server (:3000) was NEVER restarted; `prisma/`, `constants.ts`, `services.ts`, `api.ts`, `format.ts`, `finance/index.tsx`, `new-expense-page.tsx`, expenses/invoices/payments were NOT touched. PettyCashTab is NOT yet wired into Finance tabs — that integration belongs to Task 8 (finance/index.tsx renders it by import).

## Verification (actual outputs)
- `bunx tsc --noEmit 2>&1 | grep -i petty` → ZERO matches; total project errors 46, all pre-existing in files/ pm/ irms/ push/ workflows/scheduler + other agents' scripts.
- `bun run lint` → exit 0, zero output (clean).
- QA final run: **RESULT: 78 passed, 0 failed** (fund create 1000→100000c; dup 409; negative 400; TOP_UP 200 PENDING → balance frozen 100000 (§35) → approve → 120000 + ACC-CASH +20000 + INCOME ledger row; EXPENSE 45 frozen → approve → 115500 + EXPENSE ledger row; EXPENSE 5000 approve → 422 "Insufficient fund balance", balance unchanged, tx still PENDING; finance self-approve → 403, admin approves → 200 (Beta 25050→23850), second approve → 422 INVALID_TRANSITION; reject note guards 400/400 then REJECTED, balance untouched; ADJUSTMENT direction IN honored; PATCH PENDING 200 / APPROVED 422 / missing 404; reconcile counted 1150 → flagged −500c, counted 1155 → balanced, balance NEVER adjusted; receipt PNG 201 + byte-identical attachment download + replace 201 + key prefix `finance/petty-cash/{fundId}/{txId}/` + .txt magic mismatch 400 + upload on APPROVED 422 + absent 404; deactivate with pending → 409 "Settle pending transactions first."; tx on INACTIVE fund → 422; filters fund/type/status/month + bad month 400; summary shape + monthly IN + TRANSPORT category; RBAC: supervisor list 403, supervisor create 403, technician funds 403, anon 401, supervisor /users 200 (picker path), finance full access).
- Cleanup verified independently post-run: funds=0 txs=0 recons=0, PETTY_CASH ledger rows=0, PETTY_CASH audit rows=0, MinIO `finance/petty-cash/` prefix = 0 objects, ACC-CASH restored 363700→350000 (pre-test snapshot).
